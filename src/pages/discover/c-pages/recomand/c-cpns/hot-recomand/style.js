const { default: styled } = require("styled-components");

export const HotRecomandWrap = styled.div`
	.album-list {
		display: flex;
		justify-content: space-between;
		flex-wrap: wrap;
		margin-top: 20px;
	}
`;
